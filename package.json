{
  "name": "ktm",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "start": "npx tailwindcss -i ./style.css -o ./output.css --watch && npm run dev",
    "tailwind": "npx tailwindcss -i ./style.css -o ./output.css --watch",
    "init": "npm install && npm run dev",
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.4",
    "vite": "^5.3.1"
  }
}
